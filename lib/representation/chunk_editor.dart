import 'package:flutter/material.dart';

class ChunkEditor extends StatelessWidget {
  const ChunkEditor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Editor'));
  }
}
